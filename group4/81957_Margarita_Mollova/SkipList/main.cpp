#define DOCTEST_CONFIG_IMPLEMENT

#include <iostream>

#include "SkipList.h"
#include "tests.h"

int main ()
{
    doctest::Context().run();

    // SkipList skipL;
    // skipL.addElement(5);
    // skipL.addElement(12);
    // skipL.addElement(3);
    // skipL.addElement(17);
    // skipL.addElement(8);
    // skipL.addElement(34);
    // skipL.addElement(29);
    // skipL.addElement(2);
    // skipL.addElement(16);
    // skipL.addElement(21);
    // skipL.addElement(45);
    // skipL.addElement(82);
    // skipL.addElement(7);
    // skipL.addElement(0);

    // skipL.printSkipList();
    // skipL.printSkipping();

    return 0;
}